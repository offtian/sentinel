# _sre-base is a parent-only runbook (alertnames empty, never directly
# matched by Stage 1). The single fixture below confirms it loads in
# isolation; the matcher's golden runner skips it because no alert ever
# satisfies an empty alertname list with a non-zero score.
fixtures:
  - id: base-loads-in-isolation
    alert_payload_path: fixtures/_sentinel.json
    expected:
      runbook_id: null
      match_method: no_match
      required_checks_executed: []
      hypothesis_keywords: []
      forbidden_substrings_in_summary: []
