apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  labels:
    app.kubernetes.io/name: api-gateway
    app.kubernetes.io/instance: api-gateway
    team: backend
spec:
  selector:
    app.kubernetes.io/name: api-gateway
    app.kubernetes.io/instance: api-gateway
  ports:
  - port: 80
    targetPort: 80
    name: http
  type: ClusterIP
